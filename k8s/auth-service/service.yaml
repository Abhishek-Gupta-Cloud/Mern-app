apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: mern-app
  labels:
    app: auth-service
spec:
  type: ClusterIP
  selector:
    app: auth-service
  ports:
    - port: 5001
      targetPort: 5001
