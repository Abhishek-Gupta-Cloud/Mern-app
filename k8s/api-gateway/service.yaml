apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: mern-app
  labels:
    app: api-gateway
spec:
  type: ClusterIP
  selector:
    app: api-gateway
  ports:
    - port: 4000
      targetPort: 4000
